# Εγχειρίδιο Χρήσης — Koinon

Το Koinon είναι πλατφόρμα διαχείρισης συλλόγων. Κάθε σύλλογος έχει τον δικό του ιδιωτικό χώρο στη διεύθυνση `/{slug}/`. Το σύστημα διαθέτει τέσσερις ρόλους χρηστών με διαφορετικά επίπεδα πρόσβασης.

---

## Πίνακας Ρόλων

| Ρόλος        | Πρόσβαση                                                                    |
|--------------|-----------------------------------------------------------------------------|
| `master`     | Αποκλειστικά `/admin` — ανακατευθύνεται αυτόματα εκεί από οποιαδήποτε σελίδα |
| `superadmin` | Δημιουργία συλλόγων και διαχειριστών                                        |
| `club-admin` | Διαχείριση του δικού του συλλόγου μέσω του dashboard                        |
| `member`     | Πρόσβαση στην περιοχή μελών + αποδείξεις + υπόλοιπο                        |

---

## 1. Master

### Πρόσβαση
- URL: `localhost:3000/admin` (ή `koinon.app/admin` σε production)
- Ο master ανακατευθύνεται **αυτόματα** στο `/admin` από οποιαδήποτε άλλη διεύθυνση — δεν έχει πρόσβαση στο frontend dashboard ή member-area.
- Ο πρώτος master δημιουργείται χειροκίνητα: δημιουργείτε χρήστη μέσω του admin και ορίζετε `role = master` στην κονσόλα της Neon (Tables → users).

### Τι μπορεί να κάνει
- **Πλήρη διαχείριση όλων των collections** μέσω του Payload Admin Panel (`/admin`)
- Δημιουργία, επεξεργασία και διαγραφή: Συλλόγων, Χρηστών, Υπηρεσιών, Εγγραφών, Αποδείξεων, Ανακοινώσεων, Ειδοποιήσεων, Media
- Ανάθεση ρόλων σε χρήστες (superadmin, club-admin, member)
- Ορισμός `slug` για κάθε σύλλογο (αμετάβλητο μετά τη δημιουργία)
- Ενεργοποίηση/Απενεργοποίηση συλλόγων (`isActive`)

### Δημιουργία νέου συλλόγου
1. Μεταβείτε στο `/admin` → Collections → **Tenants** → Create New
2. Συμπληρώστε: `name`, `slug` (μοναδικό, πεζά, χωρίς κενά), `contactEmail`
3. Ανεβάστε λογότυπο και hero image (προαιρετικά)
4. Ορίστε `isActive: true`
5. Αποθηκεύστε

### Δημιουργία Club Admin για σύλλογο
1. `/admin` → Collections → **Users** → Create New
2. Συμπληρώστε email, firstName, lastName
3. Ορίστε `role = club-admin`
4. Επιλέξτε τον αντίστοιχο σύλλογο στο πεδίο `tenant`
5. Αποθηκεύστε — ο χρήστης μπορεί να ορίσει κωδικό μέσω "Forgot Password"

---

## 2. Superadmin

### Πρόσβαση
- Δεν έχει πρόσβαση στο `/admin` — χρησιμοποιεί τα ίδια εργαλεία με τον master μέσω του Payload REST API
- Ο ρόλος `superadmin` ανατίθεται από master

### Τι μπορεί να κάνει
- Πλήρη ανάγνωση/εγγραφή σε όλα τα δεδομένα (χωρίς περιορισμό tenant)
- Δημιουργία νέων συλλόγων και club-admins
- Διαχείριση εγγραφών και πληρωμών σε οποιονδήποτε σύλλογο

> Σε τρέχουσα υλοποίηση ο superadmin δεν διαθέτει custom dashboard UI — λειτουργεί μέσω του Payload REST API ή μελλοντικής διεπαφής.

---

## 3. Club Admin (Διαχειριστής Συλλόγου)

### Σύνδεση
1. Μεταβείτε στη διεύθυνση `/{slug}/login`
2. Εισαγάγετε email και κωδικό
3. Μετά την επιτυχή σύνδεση ανακατευθύνεστε αυτόματα στο `/{slug}/dashboard`

### Πίνακας Ελέγχου — Επισκόπηση (`/dashboard`)
Εμφανίζει τρεις βασικές στατιστικές:
- **Μέλη** — συνολικός αριθμός εγγεγραμμένων μελών
- **Ενεργές Υπηρεσίες** — υπηρεσίες με `isActive: true`
- **Ανεξόφλητες Οφειλές** — εγγραφές με `paymentStatus: unpaid`

---

### Μέλη (`/dashboard/members`)

#### Προβολή μελών
Λίστα όλων των μελών του συλλόγου με Όνομα, Email και κουμπιά ενεργειών.

#### Πρόσκληση νέου μέλους
1. Κλικ στο **+ Πρόσκληση Μέλους**
2. Συμπληρώστε: Όνομα, Επώνυμο, Email
3. Κλικ **Αποστολή Πρόσκλησης**
4. Το μέλος λαμβάνει email με σύνδεσμο για να ορίσει τον κωδικό του
5. Ο σύνδεσμος λήγει σε 48 ώρες

#### Επαναποστολή πρόσκλησης
- Αν ο σύνδεσμος έχει λήξει ή το μέλος δεν έχει ολοκληρώσει την εγγραφή
- Κλικ **Επαναποστολή** στη γραμμή του μέλους
- Δημιουργείται νέος σύνδεσμος και αποστέλλεται εκ νέου

#### Διαγραφή μέλους
1. Κλικ **Διαγραφή** στη γραμμή του μέλους
2. Εμφανίζεται επιβεβαίωση: **Ναι / Άκυρο**
3. Με επιβεβαίωση, ο λογαριασμός διαγράφεται οριστικά

---

### Υπηρεσίες (`/dashboard/services`)

#### Δημιουργία νέας υπηρεσίας
1. Κλικ στο **+ Νέα Υπηρεσία**
2. Συμπληρώστε:
   - **Τίτλος** (υποχρεωτικό) — π.χ. "Τμήμα Ενηλίκων"
   - **Εκπαιδευτής** — όνομα προπονητή/εκπαιδευτή
   - **Ενεργή υπηρεσία** — αν είναι επιλεγμένο εμφανίζεται στα μέλη
3. **Χρέωση**:
   - **Τύπος χρέωσης**: Μηνιαίο ή Ανά Συνεδρία
   - **Μηνιαίο Τέλος (€)** — εμφανίζεται αν τύπος = Μηνιαίο
   - **Τέλος ανά Συνεδρία (€)** — εμφανίζεται αν τύπος = Ανά Συνεδρία
4. **Εβδομαδιαίο Πρόγραμμα**: κλικ **+ Προσθήκη Ώρας** για κάθε χρονοθυρίδα
   - Επιλέξτε ημέρα (Δευτέρα–Κυριακή)
   - Ώρα έναρξης και λήξης
   - Χώρος (προαιρετικό) — π.χ. "Αίθουσα Α"
   - Το × αφαιρεί τη συγκεκριμένη χρονοθυρίδα
4. Κλικ **Δημιουργία Υπηρεσίας**

#### Επεξεργασία υπηρεσίας
1. Κλικ **Επεξεργασία** στην κάρτα της υπηρεσίας
2. Τροποποιήστε οποιοδήποτε πεδίο
3. Κλικ **Αποθήκευση Αλλαγών**

#### Διαγραφή υπηρεσίας
1. Κλικ **Διαγραφή** στην κάρτα
2. Επιβεβαιώστε με **Ναι**

> Η διαγραφή υπηρεσίας δεν διαγράφει τις εγγραφές μελών που αναφέρονται σε αυτή.

---

### Ανακοινώσεις (`/dashboard/announcements`)

Εμφανίζεται λίστα ανακοινώσεων με:
- **Τίτλος** και ημερομηνία δημοσίευσης
- **Δημοσιευμένη** (πράσινο) ή **Πρόχειρο** (κίτρινο)
- Καρφιτσωμένες ανακοινώσεις εμφανίζονται πρώτες

#### Δημιουργία ανακοίνωσης
1. Κλικ **+ Νέα** — ανοίγει το Payload Admin στο δημιουργία ανακοίνωσης
2. Συμπληρώστε τίτλο και περιεχόμενο (richText editor)
3. Ορίστε `status = published` για δημοσίευση ή `draft` για πρόχειρο
4. Ενεργοποιήστε **isPinned** αν θέλετε να εμφανίζεται πάντα πρώτη
5. Αποθήκευση

> Κατά τη δημοσίευση (status draft → published) αποστέλλεται αυτόματα email σε όλα τα μέλη του συλλόγου και δημιουργείται in-app ειδοποίηση.

---

### Εγγραφές μέλους — Πλάνο & Έκπτωση (`/dashboard/members/[id]/enrollments`)

Για κάθε εγγεγραμμένη υπηρεσία, κλικ στο **▼ Πλάνο** για να ορίσετε:

| Πεδίο | Επεξήγηση |
|---|---|
| Τύπος Πλάνου | Μηνιαίο ή Συνεδρίες |
| Σύνολο | Αριθμός μηνών ή συνεδριών του πλάνου (π.χ. 6 μήνες, 20 συνεδρίες) |
| Έναρξη | Ημερομηνία έναρξης του πλάνου |
| Έκπτωση | Καμία / Ποσοστό (%) / Ποσό (€) — ισχύει μόνο για αυτή την υπηρεσία |
| Αιτιολογία | Ελεύθερο κείμενο (π.χ. "Οικογενειακή έκπτωση") |

> Η **γενική έκπτωση** μέλους (που ισχύει για όλες τις υπηρεσίες) ορίζεται στο προφίλ του χρήστη μέσω του `/admin` → Users → globalDiscountType/Value.

---

### Πληρωμές (`/dashboard/payments`)

Εμφανίζεται πίνακας με τις ενεργές εγγραφές, ομαδοποιημένες σε **Μηνιαία Πλάνα** και **Πλάνα Συνεδριών**:

| Στήλη | Επεξήγηση |
|---|---|
| Μέλος | Όνομα μέλους |
| Υπηρεσία | Τίτλος υπηρεσίας |
| Πλάνο | Invoiced / σύνολο (π.χ. 2/6 μήνες) |
| Αξία | Συνολική αξία πλάνου μετά εκπτώσεων |
| Πληρωμένο | Σύνολο αποδείξεων που έχουν εκδοθεί |
| Υπόλοιπο | Αξία − Πληρωμένο (κόκκινο αν οφείλεται) |

**Φίλτρο μήνα** (‹ ›): επηρεάζει μόνο μηνιαία πλάνα — εμφανίζει εγγραφές που καλύπτουν τον επιλεγμένο μήνα.

> Εγγραφές χωρίς ορισμένο πλάνο (0 μήνες/συνεδρίες) **δεν εμφανίζονται** στον πίνακα πληρωμών.

---

### Έκδοση Απόδειξης

1. Κλικ **+ Απόδειξη** στη γραμμή του μέλους
2. Στο modal επιλέξτε τις υπηρεσίες που πληρώνει το μέλος (προεπιλεγμένες οι ανεξόφλητες)
3. Ελέγξτε/τροποποιήστε:
   - **Περίοδος / Περιγραφή** — προσυμπληρώνεται αυτόματα (π.χ. "Απρίλιος 2026" ή "12 συνεδρίες")
   - **Αριθμός συνεδριών** — για πλάνα συνεδριών (ενημερώνει αυτόματα το ποσό)
   - **Τελικό ποσό** — επεξεργάσιμο (override)
4. Συμπληρώστε: **Αριθμός Απόδειξης** (ελεύθερη μορφή, π.χ. ΑΠΟ-2026-001), **Τρόπος Πληρωμής**, **Ημερομηνία**, **Σημειώσεις**
5. Κλικ **Έκδοση Απόδειξης**
6. Η απόδειξη αποθηκεύεται και ο μέλος λαμβάνει **in-app ειδοποίηση**

---

### Ιστορικό Αποδείξεων (`/dashboard/receipts`)

Πλήρες ιστορικό όλων των εκδοθεισών αποδείξεων για τον σύλλογο:
- Αριθμός απόδειξης, μέλος, ημερομηνία, τρόπος πληρωμής, υπηρεσίες, σύνολο

---

### Αποσύνδεση
- Κλικ **Αποσύνδεση** στην επάνω δεξιά γωνία του dashboard

---

## 4. Μέλος

### Πρόσβαση — Πρώτη σύνδεση
1. Λαμβάνετε email πρόσκλησης από τον διαχειριστή του συλλόγου
2. Κλικ στον σύνδεσμο στο email
3. Ορίζετε τον κωδικό σας
4. Συνδέεστε στο `/{slug}/login`

### Επόμενες συνδέσεις
1. Μεταβείτε στο `/{slug}/login`
2. Εισαγάγετε email και κωδικό
3. Ανακατευθύνεστε αυτόματα στο `/{slug}/member-area`

---

### Αρχική (`/member-area`)

Η αρχική σελίδα εμφανίζει:

#### Κάρτα Υπολοίπου
Εμφανίζεται αν έχετε εγγεγραμμένα πλάνα:
- **Σύνολο Πλάνου** — συνολική αξία όλων των πλάνων σας (μετά εκπτώσεων)
- **Καταβληθέν** — σύνολο αποδείξεων που έχουν εκδοθεί
- **Υπόλοιπο** — ό,τι απομένει (κόκκινο αν οφείλεται, πράσινο αν εξοφλήθηκε)
- Κλικ στην κάρτα → μεταφορά στη λίστα αποδείξεων

#### Ειδοποίηση αδιάβαστων
- Μπλε banner στην κορυφή αν υπάρχουν αδιάβαστες ειδοποιήσεις
- Κλικ στο 🔔 για προβολή

#### Οι Υπηρεσίες μου
- Λίστα υπηρεσιών στις οποίες είστε εγγεγραμμένοι
- Κάθε υπηρεσία δείχνει τον εκπαιδευτή και την κατάσταση πληρωμής

#### Τελευταίες Ανακοινώσεις
- Οι 5 πιο πρόσφατες δημοσιευμένες ανακοινώσεις
- Καρφιτσωμένες ανακοινώσεις (📌) εμφανίζονται πρώτες

---

---

### Αποδείξεις (`/member-area/receipts`)

Λίστα όλων των αποδείξεων που έχουν εκδοθεί για εσάς:
- Αριθμός απόδειξης, ημερομηνία, τρόπος πληρωμής
- Ανάλυση ανά υπηρεσία (περίοδος + ποσό)
- Σύνολο ανά απόδειξη

> Κάθε νέα απόδειξη συνοδεύεται από **in-app ειδοποίηση** στο 🔔.

---

### Πρόγραμμα (`/member-area/schedule`)

Εμφανίζει το εβδομαδιαίο πρόγραμμα βάσει των εγγραφών σας:
- Ομαδοποίηση ανά ημέρα (Δευτέρα → Κυριακή)
- Κάθε χρονοθυρίδα δείχνει: ώρα, τίτλο υπηρεσίας και χώρο (αν υπάρχει)

> Αν δεν εμφανίζεται πρόγραμμα, ο διαχειριστής δεν έχει ορίσει ακόμα ωράριο στις υπηρεσίες σας.

---

### Ειδοποιήσεις (`/member-area/notifications`)

Εμφανίζει όλες τις ειδοποιήσεις σας, ταξινομημένες από την πιο πρόσφατη:

| Τύπος         | Εικονίδιο | Αιτία                                           |
|---------------|-----------|-------------------------------------------------|
| Ανακοίνωση    | 📢        | Νέα δημοσιευμένη ανακοίνωση στον σύλλογο        |
| Πληρωμή       | 💳        | Η εγγραφή σας σημειώθηκε ως ανεξόφλητη          |
| Υπηρεσία      | 🗓        | Νέα υπηρεσία προστέθηκε στον σύλλογο            |
| Γενική        | 💬        | Χειροκίνητη ειδοποίηση από τον διαχειριστή      |

- Αδιάβαστες ειδοποιήσεις εμφανίζονται με μπλε τελεία
- Διαβασμένες εμφανίζονται με μειωμένη ένταση

#### Σημείωση ανακτηθέντος (NotificationBell)
- Το 🔔 στην επάνω μπάρα ανανεώνεται αυτόματα κάθε 30 δευτερόλεπτα
- Κλικ στο κουδούνι για γρήγορη προβολή
- Κλικ ✓ σε μια ειδοποίηση για να τη σημειώσετε ως διαβασμένη

---

### Εγκατάσταση ως εφαρμογή (PWA)

Στην πρώτη είσοδο στην περιοχή μελών εμφανίζεται banner:

**"Προσθήκη στην Αρχική"**
- Κλικ **Εγκατάσταση** — η εφαρμογή προστίθεται στην αρχική οθόνη του κινητού σας
- Κλικ **Αργότερα** — το banner δεν εμφανίζεται ξανά στη συγκεκριμένη συσκευή
- Λειτουργεί σε iOS (Safari) και Android (Chrome)

---

### Αποσύνδεση
- Κλικ **Αποσύνδεση** στην επάνω δεξιά γωνία

---

## Δημόσια Σελίδα Συλλόγου (`/{slug}`)

Δεν απαιτεί σύνδεση. Εμφανίζει:
- Λογότυπο, όνομα και στοιχεία επικοινωνίας του συλλόγου
- Ενεργές υπηρεσίες με εκπαιδευτή, τίτλο, κόστος και πρόγραμμα
- Δημοσιευμένες ανακοινώσεις
- Κουμπί **Σύνδεση Μέλους** → `/{slug}/login`

---

## Συχνές Ερωτήσεις

**Ξέχασα τον κωδικό μου.**
Επικοινωνήστε με τον διαχειριστή του συλλόγου σας για να σας στείλει νέο σύνδεσμο πρόσκλησης (Επαναποστολή).

**Δεν βλέπω πρόγραμμα στη σελίδα Schedule.**
Ο διαχειριστής δεν έχει ορίσει ακόμα ωράριο στις υπηρεσίες σας, ή δεν είστε εγγεγραμμένοι σε καμία υπηρεσία.

**Έλαβα email "Ανεξόφλητη Οφειλή".**
Επικοινωνήστε με τον διαχειριστή του συλλόγου σας για να τακτοποιήσετε την πληρωμή.

**Η εφαρμογή δεν φορτώνει (offline).**
Εμφανίζεται η σελίδα "Δεν υπάρχει σύνδεση". Ελέγξτε τη σύνδεσή σας και ανανεώστε.
